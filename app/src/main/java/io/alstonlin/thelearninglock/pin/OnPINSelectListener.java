package io.alstonlin.thelearninglock.pin;

/**
 * A listener that returns a String representing then PIN the user selected
 */
public interface OnPINSelectListener {
    void onPINSelected(String PIN);
}
